# Backend API base URL (no trailing slash). Used for all API requests.
# Leave unset or empty to use same-origin (e.g. when behind a reverse proxy).
# Local development with server on port 3001:
REACT_APP_API_URL=http://localhost:3001
